import 'package:flutter/material.dart';

class FormulaWidget extends StatelessWidget {
  const FormulaWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
